# Instalación recomendada (paquete editable + extras de desarrollo):
#   pip install -e ".[dev]"
#
# Este archivo lista las dependencias directas por si preferís pip install -r.
mne>=1.6
numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3        # baseline (bandpower + regresión logística)

# Web local (MVP): subir un .edf y ver la detección
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# PyTorch: para CPU conviene el wheel dedicado (mucho más liviano que el de CUDA):
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.0

# Desarrollo / exploración
pytest>=7.4
jupyter
matplotlib
